$E \Psi = H \Psi$, $y = \beta_1x_1 + \beta_2^2x_2 + \frac{\beta_3}{2}x_3 + \varepsilon$

```{r}
head(poisson32_rep1)
```

```{r}
head(coef)
```

```{r}
coef_file = read.csv("coef.csv")
head(coef_file)
```
