packages: ~
sources: ~
targets:
  all:
    depends:
    - dat
  dat:
    depends:
    - normal16
    - poisson32
    - poisson64
  normal16:
    command: normal_dataset(n = 16)
  poisson32:
    command: poisson_dataset(n = 32)
  poisson64:
    command: poisson_dataset(n = 64)

